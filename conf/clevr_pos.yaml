# config.yaml
defaults:
  - clevr_base
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

model:
  dataset: clevr_pos
  learn_sigma: true
  num_classes: "2"

ebm:
  num_samples_per_trial: ???
  sampler_type: ULA
